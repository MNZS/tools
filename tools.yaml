---
linode:
  api: '' ## api key
  alias: '' ## full path to the shell config file where aliases can be written
  root: '' ## root password
  type: ''
  region: ''
  image: ''
  ssh_user: ''

aws:
  profile: 'default' ## insert a named profile if preferred
  alias: '' ## full path to the shell config file where aliases can be written
  template_id: '' ## launch template id tag
  template_version: '' ## launch template version

do:
  api: ## api key
  alias: '' ## full path to the shell config file where aliases can be written
  region: ''
  size: ''
  image: ''
  ssh_key: ''

## save this file as .tools.yaml in ~ for instmgr.sh
